# 🏎️ TURBOSERVIS — Профессиональный автосервис

Современный многостраничный сайт автосервиса с формой обратной связи. Дерзкий дизайн в зелёных тонах, оптимизированный для продаж.

## 🎨 Особенности дизайна

- **Логотип** в стиле турбо: зелёные полосы, современный SVG
- **Огромные hero-баннеры** под картинки автомобилей (легко добавить свои изображения)
- **Зелёная цветовая схема** (#10b981) — динамичная, автомобильная
- **Продающие секции**: преимущества, кейсы, команда, гарантии
- **Чистый код**: структурированные компоненты, переиспользуемые UI-элементы
- **Анимации**: плавные переходы и появление элементов

## 📱 Страницы

1. **Главная** (`/`) — Hero-баннер, преимущества, популярные услуги, CTA
2. **Услуги** (`/services`) — 8 услуг с детальным описанием и ценами
3. **О нас** (`/about`) — Команда, достижения, кейсы, гарантии
4. **Контакты** (`/contacts`) — Форма заявки, карта, соцсети, режим работы
5. **404** — Стильная страница ошибки

## 🛠️ Технологии

### Frontend
- **React 19** + TypeScript
- **React Router** — многостраничный роутинг
- **Tailwind CSS** — утилитарные стили
- **Lucide Icons** — автомобильные иконки
- **Vite** — быстрая сборка и HMR

### Backend
- **Express** + TypeScript
- **Zod** — валидация форм
- **CORS** настроен
- **JSON-хранилище** заявок (`backend/data/leads.json`)

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Разработка (dev)
Запускает фронтенд (`:3000`) и бэкенд (`:3001`) одновременно:
```bash
npm run dev
```

Откройте: **http://localhost:3000**

### Production сборка
```bash
npm run build
npm run start
```

Backend раздаёт статику фронтенда: **http://localhost:3001**

### Линтинг
```bash
npm run lint
```

## 📞 Контакты в проекте

- **Телефон**: 777-588-787 (из логотипа)
- **Email**: info@turboservis.ru
- **Адрес**: г. Москва, ул. Автомобильная, д. 1

*(замените на реальные в `/frontend/src/app/ui/Layout.tsx` и `/frontend/src/pages/ContactsPage.tsx`)*

## 🎯 Места для замены контента

### 1. Логотип
Файл: `frontend/public/logo.svg`  
Замените на ваш дизайн (сейчас в зелёных тонах с полосами).

### 2. Hero-баннеры (главная страница)
Файл: `frontend/src/pages/HomePage.tsx`  
Секция `<section className="relative h-[600px]">` — добавьте `<img>` с картинкой машины:

```tsx
<div className="absolute inset-0">
  <img src="/images/hero-car.jpg" className="w-full h-full object-cover" />
  <div className="absolute inset-0 bg-gradient-to-r from-dark/90 to-dark/50" />
</div>
```

### 3. Услуги
Файл: `frontend/src/data/services.ts`  
Редактируйте массив `services`: название, описание, цены, иконки.

### 4. Команда
Файл: `frontend/src/pages/AboutPage.tsx`  
Секция "Наша команда" — замените имена, должности, фото (добавьте `<img>` вместо иконки).

### 5. Карта
Файл: `frontend/src/pages/ContactsPage.tsx`  
Секция с `MapPin` — вставьте `<iframe>` Яндекс/Google карты.

## 🔧 API Endpoints

### `POST /api/leads`
Приём заявки с формы.

**Body:**
```json
{
  "name": "Иван",
  "phone": "+79001234567",
  "email": "ivan@example.com",
  "service": "Ремонт турбины",
  "message": "Нужна диагностика",
  "preferredContact": "whatsapp",
  "consent": true
}
```

**Response:**
```json
{
  "ok": true,
  "id": "abc123",
  "createdAt": "2026-01-21T12:00:00.000Z"
}
```

Заявки сохраняются в `backend/data/leads.json`.

### `GET /api/leads` (опционально)
Просмотр всех заявок (добавьте `ADMIN_TOKEN` для защиты в продакшене).

### `GET /api/health`
Проверка работы API.

## 📂 Структура проекта

```
turboservis/
├── frontend/               # Vite + React фронтенд
│   ├── public/
│   │   └── logo.svg       # Логотип TURBOSERVIS
│   ├── src/
│   │   ├── app/
│   │   │   ├── router.tsx      # React Router routes
│   │   │   └── ui/Layout.tsx   # Header + Footer
│   │   ├── components/
│   │   │   ├── ui/             # Button, Card (переиспользуемые)
│   │   │   ├── icons/          # Автомобильные SVG иконки
│   │   │   └── LeadForm.tsx    # Форма заявки
│   │   ├── pages/              # Страницы сайта
│   │   ├── data/services.ts    # Список услуг
│   │   ├── lib/api.ts          # API клиент
│   │   └── index.css           # Tailwind + анимации
│   └── vite.config.ts          # Proxy /api → :5174
│
├── backend/                # Express API
│   ├── src/
│   │   ├── index.ts            # Основной сервер
│   │   ├── storage/            # Сохранение заявок
│   │   └── validation/         # Zod-схемы
│   └── data/leads.json         # JSON-хранилище
│
├── package.json            # Root workspaces
└── README.md
```

## 🎨 Цветовая схема

| Название | HEX | Применение |
|----------|-----|------------|
| Primary 500 | `#10b981` | Кнопки, акценты, логотип |
| Primary 600 | `#059669` | Hover-состояния |
| Dark | `#0a0a0a` | Фон |
| Dark 50 | `#1a1a1a` | Секции, карточки |

Настройка: `frontend/tailwind.config.js`

## 🚀 Дальнейшая работа

### Что можно улучшить:
1. **Добавить реальные фото** автомобилей, команды, кейсов
2. **Интегрировать карты** (Яндекс/Google Maps)
3. **Подключить метрики** (Яндекс.Метрика, Google Analytics)
4. **Email-уведомления** при новой заявке (Nodemailer)
5. **База данных** вместо JSON (PostgreSQL, MongoDB)
6. **Админ-панель** для просмотра заявок
7. **SEO-оптимизация** (meta-теги, sitemap, robots.txt)
8. **Мобильное меню** (бургер для навигации на мобилках)

---

## 📝 Лицензия

Проект создан для TURBOSERVIS. Все права защищены.

---

**Сделано с 💚 и 🔧**
